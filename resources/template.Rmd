---
title: "Simple document"
output: html_document
---

I'm an R Markdown document! 

Here's a code chunk:

```{r}
samp = rnorm(100)
length(samp)
```

I can take the mean, too: the
mean is `r mean(samp)`.